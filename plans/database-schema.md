# Database Schema Design

## Oracle SQL Developer Schema

### USERS Table
```sql
CREATE TABLE USERS (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    email VARCHAR2(255) UNIQUE NOT NULL,
    password_hash VARCHAR2(255) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### TASKS Table
```sql
CREATE TABLE TASKS (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    title VARCHAR2(255) NOT NULL,
    status VARCHAR2(50) DEFAULT 'Backlog' CHECK (status IN (
        'Backlog', 'Todo', 'In Progress', 'Review', 
        'Testing', 'Done', 'Blocked', 'Archived'
    )),
    assignee_id NUMBER NULL,
    due_date TIMESTAMP NULL,
    created_by NUMBER NOT NULL,
    position NUMBER DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    CONSTRAINT fk_tasks_assignee FOREIGN KEY (assignee_id) 
        REFERENCES USERS(id) ON DELETE SET NULL,
    CONSTRAINT fk_tasks_created_by FOREIGN KEY (created_by) 
        REFERENCES USERS(id) ON DELETE CASCADE
);
```

### ASSIGNMENT_HISTORY Table
```sql
CREATE TABLE ASSIGNMENT_HISTORY (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    task_id NUMBER NOT NULL,
    old_assignee_id NUMBER NULL,
    new_assignee_id NUMBER NULL,
    changed_by NUMBER NOT NULL,
    changed_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    CONSTRAINT fk_assignment_task FOREIGN KEY (task_id) 
        REFERENCES TASKS(id) ON DELETE CASCADE,
    CONSTRAINT fk_assignment_old_assignee FOREIGN KEY (old_assignee_id) 
        REFERENCES USERS(id) ON DELETE SET NULL,
    CONSTRAINT fk_assignment_new_assignee FOREIGN KEY (new_assignee_id) 
        REFERENCES USERS(id) ON DELETE SET NULL,
    CONSTRAINT fk_assignment_changed_by FOREIGN KEY (changed_by) 
        REFERENCES USERS(id) ON DELETE CASCADE
);
```

### WORK_LOGS Table
```sql
CREATE TABLE WORK_LOGS (
    id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    task_id NUMBER NOT NULL,
    user_id NUMBER NOT NULL,
    hours_logged NUMBER(5,2) NOT NULL CHECK (hours_logged > 0),
    description VARCHAR2(1000),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    CONSTRAINT fk_worklogs_task FOREIGN KEY (task_id) 
        REFERENCES TASKS(id) ON DELETE CASCADE,
    CONSTRAINT fk_worklogs_user FOREIGN KEY (user_id) 
        REFERENCES USERS(id) ON DELETE CASCADE
);
```

## Indexes
```sql
CREATE INDEX idx_tasks_status ON TASKS(status);
CREATE INDEX idx_tasks_position ON TASKS(position);
CREATE INDEX idx_worklogs_task_id ON WORK_LOGS(task_id);
CREATE INDEX idx_assignment_history_task_id ON ASSIGNMENT_HISTORY(task_id);
CREATE INDEX idx_tasks_assignee_id ON TASKS(assignee_id);
CREATE INDEX idx_tasks_created_by ON TASKS(created_by);
```

## Views for Reporting
```sql
CREATE VIEW TASK_REPORT_VIEW AS
SELECT 
    t.id,
    t.title,
    t.status,
    u.email as assignee_email,
    creator.email as created_by_email,
    COALESCE(SUM(wl.hours_logged), 0) as total_hours
FROM TASKS t
LEFT JOIN USERS u ON t.assignee_id = u.id
LEFT JOIN USERS creator ON t.created_by = creator.id
LEFT JOIN WORK_LOGS wl ON t.id = wl.task_id
GROUP BY t.id, t.title, t.status, u.email, creator.email;
```

## Stored Procedure
```sql
CREATE OR REPLACE PROCEDURE LOG_ASSIGNMENT_CHANGE(
    p_task_id IN NUMBER,
    p_old_assignee_id IN NUMBER,
    p_new_assignee_id IN NUMBER,
    p_changed_by IN NUMBER
) AS
BEGIN
    INSERT INTO ASSIGNMENT_HISTORY (task_id, old_assignee_id, new_assignee_id, changed_by)
    VALUES (p_task_id, p_old_assignee_id, p_new_assignee_id, p_changed_by);
END;
/
```

## Setup Steps
1. Connect to Oracle SQL Developer using: `Data Source=DESKTOP-8RJAQKG;Integrated Security=True;`
2. Execute the schema creation scripts in order
3. Verify tables are created successfully
4. Test with sample data insertion

## Data Validation Rules
- Task title: Required, max 255 characters
- Assignee: Nullable (unassigned tasks allowed)
- Work hours: Decimal, positive only
- Status: Must be one of 8 predefined columns
- Email: Unique constraint for users

## Relationships Diagram
```
USERS
  │
  ├─── created_by (TASKS.created_by)
  ├─── assignee_id (TASKS.assignee_id)
  ├─── user_id (WORK_LOGS.user_id)
  ├─── changed_by (ASSIGNMENT_HISTORY.changed_by)
  ├─── old_assignee_id (ASSIGNMENT_HISTORY.old_assignee_id)
  └─── new_assignee_id (ASSIGNMENT_HISTORY.new_assignee_id)

TASKS
  │
  ├─── task_id (WORK_LOGS.task_id)
  └─── task_id (ASSIGNMENT_HISTORY.task_id)